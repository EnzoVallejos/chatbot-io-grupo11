# Pydantic actualizado: las versiones < 2.10 explotan al evaluar
# anotaciones de tipo en Python 3.14 (nuevo annotationlib / PEP 749).
pydantic>=2.10

# Núcleo de LangChain 0.3.x (compatible con Python 3.14)
langchain>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-text-splitters>=0.3,<0.4

# Integraciones dedicadas (reemplazan a las clases deprecadas de langchain-community)
langchain-ollama>=0.2          # antes: langchain_community.llms.Ollama
langchain-chroma>=0.1.4        # antes: langchain_community.vectorstores.Chroma
langchain-huggingface>=0.1     # antes: langchain_community.embeddings.HuggingFaceEmbeddings

# Base vectorial y embeddings
chromadb>=0.5
sentence-transformers>=3.0

# Cliente HTTP de Ollama (lo usa langchain-ollama por debajo)
ollama>=0.3
